/* 
현재 사용하지 않습니다.

function Period({ start_at, end_at }) {
  return (
    <div className="text-gray-200 text-justify">
      활동일자: {`${start_at}${' ~ ' + (end_at ? end_at : '')}`}
    </div>
  )
}

export default Period
*/
